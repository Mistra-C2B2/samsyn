# Database
DATABASE_URL=postgresql://samsyn:samsyn@samsyn-db:5432/samsyn

# Test Database (separate from dev to prevent data loss during tests)
TEST_DATABASE_URL=postgresql://samsyn:samsyn@samsyn-db:5432/samsyn_test

# Clerk Authentication
CLERK_SECRET_KEY=sk_test_...
CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_WEBHOOK_SECRET=whsec_...
CLERK_JWKS_URL=https...

# Frontend URL (for CORS)
# List all origins that should be allowed to access the backend API
# Frontend runs on port 3000 (configured in vite.config.ts)
# clerk.shared.lcl.dev is used by Clerk for embedded auth components (optional)
FRONTEND_URL=http://localhost:3000,https://clerk.shared.lcl.dev

# TiTiler (for GeoTIFF/COG tile serving)
TITILER_URL=http://localhost:8001

# Global Fishing Watch API Token (optional)
# Required if you want to use GFW data layers
# Get from https://globalfishingwatch.org/our-apis/
# IMPORTANT: Keep this token server-side only (never expose to frontend)
GFW_API_TOKEN=your_gfw_token_here

# HTTP Client Connection Pool Settings
# Used for external HTTP requests (TiTiler, WMS servers, external APIs)
# Prevents connection exhaustion when loading maps with many tiles
HTTP_POOL_CONNECTIONS=100        # Max total connections in pool
HTTP_POOL_MAXSIZE=100            # Max connections per host
HTTP_TIMEOUT=30.0                # Request timeout in seconds
HTTP_KEEPALIVE_EXPIRY=30.0       # Keep connections alive for N seconds
